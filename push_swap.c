#include "push_swap.h"
#include <stdio.h>

int	main(int argc, char **argv)
{
	t_list	*stack_a;
	printf("%d\n", argc);
	printf("%s\n", argv[1]);
	printf("%s\n", argv[2]);
//	if (input_handler(argv))
//		return (1);
	return (0);
}
